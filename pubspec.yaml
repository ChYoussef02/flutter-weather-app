name: weather_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_lints: ^2.0.0

  cupertino_icons: ^1.0.2
  http: ^0.13.3
  geolocator: ^8.0.0
  geocoding: ^2.0.0
  lottie: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/sunny.json
    - assets/cloudy.json
    - assets/rainy.json
